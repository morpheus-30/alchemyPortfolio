import {motion} from 'framer-motion';
import styles from '../style'
import KeyboardCanvas from './space_boi';

const Hero = () => {
    return (
        <section className='w-full h-screen snap'>
            <KeyboardCanvas />
        </section >
    )
}

export default Hero; 
